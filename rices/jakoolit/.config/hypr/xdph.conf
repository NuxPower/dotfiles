# Loom/Zen screen sharing: DMA-BUF negotiation fails on this Intel/Hyprland
# setup and causes the PipeWire stream to freeze. Use the portal's stable
# shared-memory path instead and keep recording load reasonable.
screencopy {
    force_shm = true
    max_fps = 60
}
